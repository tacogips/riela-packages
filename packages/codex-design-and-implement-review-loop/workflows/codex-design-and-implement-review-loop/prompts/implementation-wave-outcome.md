Aggregate the native implementation wave before serial reconciliation.

Use only runtimeVariables.fanoutJoin or the joined inbox payload and the executed branch payloads supplied in this input. Do not run commands, inspect repository state, edit files, or invent missing dependency evidence.

The branch-local `implementation-progress-check` owns classification, and that classification is scoped to the branch's assigned plan contract. The committed manifest DAG and plan text own task allocation: work explicitly assigned to a pending downstream dependent plan is not an unresolved requirement of the current predecessor. Do not reinterpret a raw Step 6 summary or invent a second progress policy here. Set `implementation_blocked` to true when any `fanoutJoin.branches[]` terminal output from that gate has `implementation_blocked: true` or `status: "blocked"`. Preserve that branch's plan ID, blocker type, blockers, unresolved branch-owned material requirements, and concrete resume criteria. These are dependency, no-progress, or materially-unverified outcomes, not successful implementations: do not send them through test-integrity review, adversarial review, reconciliation, integration review, documentation, or git finalization. Preserve successfulPlanIds only as diagnostic work-in-progress; do not claim those plans are accepted. Never add a wave blocker merely because a downstream-owned plan remains pending; the native dependency wave intentionally handles that work after predecessor acceptance.

Set `implementation_blocked` to false only when no branch reported an implementation blocker. Ordinary failed or cancelled branches remain eligible for the existing reconciliation and selective-redispatch path and do not by themselves set this discriminator.

Set `partial_success` to true only when `implementation_blocked` is true and at least one successful plan candidate has complete branch evidence and hashes. A partial success must continue through serial reconciliation and independent integration review: only that review may promote those candidates into the complete `acceptedPlanIds` set, while blocked IDs remain pending for a later native retry. Do not promote an incomplete, blocked, failed, cancelled, or evidence-missing branch. Set `partial_success` false for a blocked-only wave so it terminates with its actionable handoff. Preserve the successful branch IDs and native evidence references in the payload; a later dispatch must receive integration-review's accepted IDs and skip them, retrying only pending blocked work. The no-progress fingerprint guard still applies to the blocked retry; do not evade it by relabeling unchanged work as successful.

Return JSON `{implementation_blocked,partial_success,blockedPlanIds,successfulPlanIds,blockers,resumeCriteria}`. When blocked, include at least one blocked plan, blocker, and resume criterion. When unblocked, return `partial_success:false` and empty blockedPlanIds, blockers, and resumeCriteria.
