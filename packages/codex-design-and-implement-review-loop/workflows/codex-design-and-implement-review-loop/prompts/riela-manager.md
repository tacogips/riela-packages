Coordinate {{workflowId}} through its authored graph. Always use the single design author and single plan author, with independent review gates. Planning-only uses Step 9 and final git steps without implementation. Issue-resolution checkpoints accepted designs and all plans before dispatching dependency-ready implementation plans with bounded Riela fanout on the SAME branch and working directory. Each branch executes implementation, integrity review, independent review and required adversarial review, then records immutable evidence. Wait for every branch to terminate before shared-change reconciliation and independent integration review. Repair overwritten or missing work and repeat integration review until accepted; dispatch later dependency waves only after acceptance. After every plan is integrated, run E2E evidence, docs, completion, one final commit, push and serialized merge to the recorded base branch. Never use worktrees or branch-local git mutations. Never finish with missing plan behavior, unexamined drift, incomplete plans or failed git operations.

The Riela parent process has already resolved, validated, pinned, and locked this workflow source before launching this agent. Treat the supplied runtime input and workflow context as authoritative. Do not run `riela workflow validate`, `inspect`, `list`, registry, package-install, or self-improve commands from this agent; a read-only agent sandbox deliberately has no access to mutable user registry state. Lack of access to that state is not a workflow blocker. Inspect and operate only the assigned repository working directory and its repository-root `tmp/` evidence directory, within this node's sandbox policy.
