Prepare a checkpoint ONLY after the single design and plan authors and independent reviewers accept all plans. Do not implement here.
For a resumed task, prefer updating its existing dispatch manifest over creating a duplicate. If a new manifest is necessary and a historical manifest is also changed in the same checkpoint, the new manifest must be the only newly added dispatch manifest. The downstream dispatcher selects from the commit's changed files, not from session history.
The dispatch manifest MUST contain a top-level `evidenceRoot` string equal to the repository-relative `tmp/<task-id>` for its `taskId`. Return that same value in the checkpoint payload. Reopen the written manifest and confirm the field is present, non-empty, and identical before requesting the Git commit; the downstream deterministic dispatcher reads this field from the committed manifest, not from the checkpoint message.
Read the intake git context, accepted design, and accepted plans. Create one repository-relative JSON dispatch manifest under impl-plans/active/<task-id>-dispatch.json. Use a unique task-id. It contains taskId, workflowMode, implementationBranch, baseBranch, remote, originalHead, designDocPaths, evidenceRoot, reviewContext, and plans: [{planId, planPath, dependsOn, writePaths, sharedPaths, acceptanceCriteria, verification}]. Each plan's `verification` MUST be an array of non-empty command strings, not an object; keep prose evidence requirements in the plan text and reviewContext. The manifest must reference every accepted design document and every accepted plan, including accepted files that are unchanged from HEAD. `reviewContext` is a concise, evidence-backed object containing issueReference, userProblem, requiredOutcomes, nonGoals, constraints, designDecisionsAndRationale, intentionalTradeoffs, supportedEdgeCases, outOfScopeEdgeCases, and sourcePaths. `issueReference` may be a nonempty string or an object identifying the accepted request, such as `{repository, draftPR, intakeCommunication}` with a positive draftPR; do not write a repository-only object. Derive it from the accepted intake/design/plan artifacts without inventing requirements. Plans must have unique IDs and safe repository-relative paths. Dependencies must resolve and form a DAG. Prefer independent file ownership, but explicitly record unavoidable shared files and intended edits. Put tightly coupled changes in the same plan or successive waves. Shared indexes, lockfiles and broad formatting belong to the serial reconciliation phase. Each plan has its own progress file; do not let workers edit a shared manifest.
Reject an empty plan set for issue-resolution. Record a unique evidenceRoot under repository tmp/<task-id>; keep it untracked, and use per-plan and per-attempt subdirectories. Verify HEAD/branch still match intake. Preserve unrelated tracked/untracked changes and staged entries: do not incorporate them into this checkpoint. If existing staged entries prevent an exact scoped commit, report blocked rather than clearing the index.

After writing the manifest, determine the exact repository-relative accepted checkpoint paths that differ from HEAD, considering staged, unstaged, and untracked state. `committedFiles` must contain exactly the new or modified dispatch manifest plus only those accepted design/plan files with actual changes. Never include an accepted design/plan path merely because the manifest references it: unchanged paths are already present in the checkpoint parent and cause `riela/git-commit` to reject the prepared staged set as a mismatch. Exclude unrelated changed paths. Every `committedFiles` entry must be changed from HEAD, and every changed accepted design/plan/manifest path intended for this checkpoint must be listed exactly once.

Return adapter JSON payload {checkpoint_blocked,status,commitMessage,committedFiles,manifestPath,evidenceRoot,blockers}. For a normal checkpoint return `checkpoint_blocked: false`, `status: "ready"`, and `blockers: []`. Do not run git yourself. Persist the manifest location in the accepted output for audit; downstream dispatch reads the checkpoint commit's changed files. Never guess the newest manifest in the repository. If the exact changed-path set cannot be established, unrelated staged entries prevent the scoped commit, or `committedFiles` would be empty, return `checkpoint_blocked: true`, `status: "blocked"`, an empty commitMessage/committedFiles, and concrete blockers. A unique newly written manifest normally ensures the checkpoint is not a no-op; never request an empty commit.
