Finish one native Riela fanout branch. Your assigned contract is runtimeVariables.implementation or fanoutItem; the stable runtime identity is fanoutBranchId (not a Git branch).
Read accepted branch reviews and your intended changes. Native changeTracking captures before/after each node and exposes the most recent immutable snapshot as runtimeVariables.fanoutChangeEvidencePath. Refer to that artifact and retain your per-edit intent/progress evidence. Do not call a helper script or manufacture hashes.
Compare fresh shared files with the intended behavior and earlier evidence. Record missing changes or deferred repair requests with concrete paths/tests. Never blindly restore an old snapshot or call a drifting file harmless without evidence. A branch's acceptance is provisional until the serial integration review checks the combined tree.
Return adapter JSON payload {planId,planPath,changedFiles,intendedChanges,acceptanceCriteria,verification,overwriteObservations,repairRequests,reviewDecisions,evidencePaths}. If the progress gate supplied `baselineReviewPending`, carry its exact failed command, nonzero exit, comparison/log paths and the independent test-integrity/adversarial dispositions in the branch output; do not relabel the aggregate as passed. Include this branch's runtime snapshot path. Do not stage, commit, push, merge, switch Git branches, create worktrees or edit shared indexes. The runtime captures the final node snapshot and ends the branch before the parent's join.
