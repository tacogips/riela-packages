Prepare the complete committed plan manifest for native Riela fanout. Do not implement, spawn agents, run nested Riela, or manually compute dependency-ready waves.
This is a bounded projection step, not an investigation or review. Do not search the repository, inspect examples, reread skills, run tests, or explore unrelated files. Use only the direct inbox payload, `git rev-parse HEAD`, the committed-files list from plan-git-commit, the one exact checkpoint manifest named by that list (or the exact manifestPath supplied by this run's integration-review), and prior acceptedPlanIds from that direct input. Read each of those at most once. Perform the projection with one bounded JSON command or equivalent in-memory transformation, validate the required fields, and return immediately.
Read this run's exact plan-checkpoint manifestPath and successful plan-git-commit. Verify every design/plan/manifest was committed; record checkpointCommit. Preserve the recorded implementationBranch, baseBranch and remote. Never guess a manifest from repository timestamps. HEAD must stay at the checkpoint through all implementation waves.
Read prior accepted integration-review records for THIS run. A plan is complete only after independent combined-tree acceptance. Failed branches and repaired-but-unreviewed work are not completed. Inspect interrupted shared work and preserve evidence; never reset or reapply an old whole-file snapshot.
Return adapter JSON payload {implementationItems, acceptedPlanIds, manifestPath, checkpointCommit}. implementationItems contains EVERY manifest plan in stable input order, including previously accepted plans; each item contains {planId,planPath,dependsOn,writePaths,sharedPaths,trackedPaths,acceptanceCriteria,verification,reviewContext,manifestPath,checkpointCommit,implementationBranch,baseBranch,remote,evidenceRoot}. Copy the manifest's complete `reviewContext` into every item because branch inboxes do not inherit parent history. Reject dispatch when reviewContext or its sourcePaths are missing rather than asking branch reviewers to infer intent from incomplete context. trackedPaths is the deduplicated union of all exact writePaths/sharedPaths and the plan's progress file; no directory paths, symlinks, .git, absolute paths or traversal.
The built-in fanout.dependencies validates unique IDs, dependencies and cycles, skips acceptedPlanIds, and dispatches only dependency-ready fanout branches. fanout.changeTracking records immutable file contents and hashes before/after workflow nodes and exposes fanoutChangeEvidencePath inside each branch. The native join aggregates branchId/status/output/sessionId, pendingBranchIds, dispatchedBranchIds and changeEvidence including artifactPaths and drift observations. Native tracking is cooperative node-boundary observation, not a lock or proof against all overwrites inside an agent call.
When no pending plans remain, dispatch should not be called: report the invalid route instead of declaring an empty issue-resolution run successful. Preserve every item's full contract because branch inboxes do not inherit the parent's prior step messages.
