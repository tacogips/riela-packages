All parallel workers have stopped; final commit and push have succeeded. Perform the authorized serialized publication handoff only now.
This is the sole elevated-sandbox agent step because a Git worktree's shared metadata and the remote are outside a workspace-write sandbox. Use that access only for the recorded repository, exact commit, base branch, authorized PR handoff or required verification and non-force push; do not inspect unrelated local files or credentials.
Read this run's recorded intake/checkpoint git context and successful final git outputs. Do not guess a base branch from main/master or current remote defaults. Verify the implementation commit is the actual pushed commit and that the tree/index contain no unrelated changes that a checkout or merge could disturb.
Before changing the base branch, inspect the effective workflow input for an explicit PR-only instruction and query open pull requests for the exact recorded repository and implementation branch. If either says PR handoff, do not checkout, merge, commit, or push the base. Require exactly one open PR whose head branch and head commit equal the verified pushed implementation branch/commit. If the recorded base branch differs from the implementation branch, require the PR base to equal that recorded base; if they are the same, require the PR base to equal an explicitly requested PR target or the repository's verified default branch, and to differ from the head. An explicit PR-only instruction with no matching PR is blocked until a Draft PR is created; an unavailable PR lookup, mismatched head/base, or ambiguous PR set is blocked, never a reason to fall through to merge. Return mergeStatus:"pr-open", basePushStatus:"not-requested", pullRequestURL, pullRequestNumber, pullRequestDraft, pullRequestBaseBranch, and verification of the remote head and PR identity. An existing open PR is review ownership, not authorization to merge it. Do not mark the base integrated.
If implementationBranch equals baseBranch, verify its remote ref contains the final commit and return mergeStatus:"already-on-base". Do not create an unnecessary merge.
Otherwise fetch the recorded remote and merge the exact verified implementation commit into the recorded base branch, with no worktree and no force push. Do not silently switch to an unrelated repository/branch. Reconcile a moved remote base first. Preserve all existing base changes. If a merge conflicts, inspect both intents, repair missing/overwritten behavior, run combined verification and an independent Riela review via the existing workflow revision route before finalizing; never use ours/theirs wholesale. For this node, an unresolved conflict or a change requiring new review must return a failed/blocked operation (never success), preserve evidence and allow explicit recovery; do not self-approve a repair. A clean merge must still run the planned integrated checks before commit/push; use --no-commit --no-ff where a merge commit is needed. If checks fail, leave the operation recoverable and report failure.
Push the base branch normally and verify remote reachability of the implementation commit. On restart inspect ancestry and remote refs before repeating: skip already-completed merge/push operations. Never reset, force push, delete branches or discard unrelated files. Planning-only also integrates only its accepted documentation commit.
Return adapter JSON payload {implementationBranch,baseBranch,remote,implementationCommit,mergeCommit,mergeStatus,basePushStatus,verification}, plus pullRequestURL/pullRequestNumber/pullRequestDraft/pullRequestBaseBranch for a PR handoff. A policy/remote/protection failure must fail this step and prevent workflow-output; report the failure with `mergeStatus:"blocked"` rather than inventing successful publication evidence. The output contract rejects a blocked payload.
