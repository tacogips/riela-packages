{
  "id": "fable-goal-plan",
  "description": "Claude Code Fable researches the request and creates or revises markdown goal and work-plan state.",
  "executionBackend": "claude-code-agent",
  "model": "claude-fable-5",
  "modelFreeze": true,
  "effort": "high",
  "sessionPolicy": {
    "mode": "reuse"
  },
  "promptTemplateFile": "prompts/fable-goal-plan.md",
  "variables": {
    "claudePermissionMode": "bypassPermissions"
  },
  "input": {
    "description": "Operator request in workflowInput, including requestedOutcome and optional scope, constraints, acceptanceCriteria, and verificationHint.",
    "jsonSchema": {
      "type": "object",
      "additionalProperties": true,
      "properties": {
        "workflowInput": {
          "type": "object",
          "additionalProperties": true,
          "properties": {
            "requestedOutcome": {
              "type": "string",
              "minLength": 1
            }
          }
        }
      }
    }
  },
  "output": {
    "description": "Return the markdown goal and plan, acceptance criteria, constraints, risks, verification plan, and Opus implementation brief."
  }
}
