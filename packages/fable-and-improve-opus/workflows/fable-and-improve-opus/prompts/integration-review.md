Independently review the stable shared-branch result after all workers stopped and reconciliation finished.
Read the committed design and ALL plans (including earlier waves), joined branch intent/evidence, per-edit and accepted snapshots, reconciliation repairs, and actual current files. Verify each acceptance criterion and missing-change detector result; do not accept only because tests pass or hashes match. Review the combined diff for semantic conflicts, removed tests, missing implementations and out-of-scope changes. Run focused checks where needed. If high/mid issues, unresolved drift, unverified overwrite repairs, unaccounted incomplete wave work or missing evidence remain, return when.needs_revision=true and concrete repair feedback. Do not edit source yourself.
On acceptance persist an immutable wave acceptance record under the run evidenceRoot containing integrated plan IDs, current hashes, test results and artifact paths. Return payload and matching when flags {needs_revision:false, plans_remaining:<whether manifest plans remain>}, plus acceptedPlanIds, pendingPlanIds, manifestPath, evidencePaths, findings, verification. Dependency completion is based on these accepted records. If pending plans exist but cannot become ready, report the invalid dependency rather than declaring complete. Final acceptance requires every plan's intended behavior survives on the combined tree.

Mirror needs_revision and plans_remaining in both when and payload. Include payload.accepted:true only on acceptance; final acceptance requires both flags false. Keep accepted:false on revision. These fields are authoritative runtime finalization evidence.

Use native fanoutJoin.dispatchedBranchIds and branchId records to validate candidate completeness. Failed branches remain pending and can be retried; accept only fully reviewed successful candidates. Return the complete acceptedPlanIds set for the next native dependency dispatch. If every branch failed and no candidate can be accepted, fail with a recovery diagnostic rather than accepting an empty wave. On final acceptance every manifest plan must be accepted. Check the entire combined implementation against the design.
