FROM python:3.12-slim

RUN python -m pip install --no-cache-dir pymupdf==1.24.14

WORKDIR /workspace
COPY pdf_to_images.py /usr/local/bin/pdf-to-images
RUN chmod +x /usr/local/bin/pdf-to-images

ENTRYPOINT ["pdf-to-images"]
