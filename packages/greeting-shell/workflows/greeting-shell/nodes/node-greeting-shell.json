{
  "id": "greeting-shell",
  "description": "Run a local Bash script and output a greeting with the current datetime.",
  "modelFreeze": false,
  "nodeType": "command",
  "variables": {},
  "command": {
    "scriptPath": "scripts/greeting.bash",
    "argvTemplate": ["{{workflowInput.name}}"],
    "envTemplate": {
      "GREETING_INDEX": "{{workflowInput.greetingIndex}}",
      "GREETING_TIMEZONE": "{{workflowInput.timezone}}"
    }
  },
  "output": {
    "description": "Return greeting text and datetime generated by the local Bash script."
  }
}
